export default function RoleFeaturePage({ title }: { title: string }) {
  return (
    <section className="feature-page">
      <p className="eyebrow">Coming next</p>
      <h2>{title}</h2>
      <p>
        This protected area is ready for its API and school-management workflow.
      </p>
    </section>
  );
}
